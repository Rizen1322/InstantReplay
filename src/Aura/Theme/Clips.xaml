<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:c="clr-namespace:Aura.Controls"
                    xmlns:lib="clr-namespace:Aura.Core.Library"
                    xmlns:v="clr-namespace:Aura.Views">

    <!-- Своя копия кривой: StaticResource ищется в пределах этого словаря -->
    <BackEase x:Key="Spring" EasingMode="EaseOut" Amplitude="0.32"/>

    <!-- Кнопка-обёртка карточки: только курсор и клик, никакого фона -->
    <Style x:Key="ClipCardButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent"><ContentPresenter/></Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Карточка записи. Пока кадр не загрузился, на её месте лежит спокойная
         заглушка — сетка не «прыгает» и не мигает белым. -->
    <DataTemplate x:Key="ClipCard" DataType="{x:Type lib:ClipItem}">
        <Button Style="{StaticResource ClipCardButton}" Margin="0,0,12,14"
                ToolTip="{Binding FileName}"
                c:DragFile.Path="{Binding FullPath}">
            <Button.ContextMenu>
                <!-- У каждого пункта своя иконка: строчки одинаковой длины без опоры
                     для глаза читались как сплошной текст, и «Удалить» находили не
                     с первого раза. -->
                <ContextMenu Style="{DynamicResource TrayMenu}">
                    <MenuItem Header="Открыть" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Open}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Play}" Size="14" Filled="True"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Показать в папке" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Reveal}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.FolderOpen}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <!-- Только у скриншотов: копируется сама картинка. У видео пункта нет —
                         путь в буфере никому не пригодился, а файл и так перетаскивается мышью. -->
                    <MenuItem Header="Копировать" Style="{DynamicResource TrayMenuItem}"
                              Visibility="{Binding ScreenshotVisibility}"
                              Command="{x:Static v:ClipCommands.CopyImage}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Copy}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="SelectItem" Header="Выделить" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.SelectItem}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.CheckSquare}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Редактировать в Aura" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Edit}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Scissors}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Сжать для Discord" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Compress}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Box}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator Style="{DynamicResource TrayMenuSeparator}"/>
                    <MenuItem Header="Переименовать" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Rename}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Pencil}" Size="14"
                                    Foreground="{DynamicResource Tx2Brush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Удалить в корзину" Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.Delete}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Trash}" Size="14"
                                    Foreground="{DynamicResource RecBrush}"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <!-- Пункты для выделенной пачки: появляются, когда выделено больше одной -->
                    <Separator x:Name="ManySeparator" Style="{DynamicResource TrayMenuSeparator}" Visibility="Collapsed"/>
                    <MenuItem x:Name="DeleteManyItem" Header="Удалить выделенные" Visibility="Collapsed"
                              Style="{DynamicResource TrayMenuItem}"
                              Command="{x:Static v:ClipCommands.DeleteMany}">
                        <MenuItem.Icon>
                            <c:Icon Data="{DynamicResource Ico.Trash}" Size="14"
                                    Foreground="{DynamicResource RecBrush}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </Button.ContextMenu>
            <StackPanel>
                <c:Aspect>
                <Border x:Name="Shot" CornerRadius="8" ClipToBounds="True"
                        RenderTransformOrigin="0.5,0.5">
                    <Border.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ShotScale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="ShotShift" Y="0"/>
                        </TransformGroup>
                    </Border.RenderTransform>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FF16181C" Offset="0"/>
                            <GradientStop Color="#FF0E0F12" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <c:Icon Data="{DynamicResource Ico.Film}" Size="24" Opacity="0.35"
                                Foreground="{DynamicResource Tx3Brush}"
                                HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <Image Source="{Binding Thumbnail}" Stretch="UniformToFill"/>

                        <Border BorderThickness="1" BorderBrush="#1AFFFFFF" CornerRadius="8" IsHitTestVisible="False"/>

                        <!-- Рамка выделения: в том же Grid и с тем же скруглением, что
                             и превью, поэтому ложится ровно по краю кадра при любом
                             размере карточки и вместе с ним же масштабируется. -->
                        <Border x:Name="SelectionRing" Opacity="0" IsHitTestVisible="False"
                                BorderThickness="2" BorderBrush="{DynamicResource AccentBrush}"
                                CornerRadius="8"/>

                        <!-- Галочка выделения: рамки мало, когда карточек много -->
                        <Border x:Name="Check" Visibility="Collapsed" IsHitTestVisible="False"
                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"
                                Width="22" Height="22" CornerRadius="6"
                                Background="{DynamicResource AccentBrush}">
                            <c:Icon Data="{DynamicResource Ico.Check}" Size="13" Foreground="{DynamicResource AccentInkBrush}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <!-- Длительность: единственная метка поверх кадра, она функциональная -->
                        <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="6"
                                Background="#CC08090B" CornerRadius="4" Padding="5,1,5,2"
                                Visibility="{Binding DurationVisibility}">
                            <TextBlock Text="{Binding DurationText}" FontSize="11" FontWeight="SemiBold" Foreground="#FFF2F3F5"
                                       FontFamily="{DynamicResource DispFont}"/>
                        </Border>

                        <!-- кружок Play при наведении (у скриншотов скрыт) -->
                        <Border x:Name="Play" Width="40" Height="40" CornerRadius="20" Opacity="0"
                                Visibility="{Binding PlayVisibility}"
                                Background="#B308090B" BorderBrush="#33FFFFFF" BorderThickness="1"
                                HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform><ScaleTransform x:Name="PlayScale" ScaleX="0.82" ScaleY="0.82"/></Border.RenderTransform>
                            <c:Icon Data="{DynamicResource Ico.Play}" Size="16" Filled="True" Foreground="White"
                                    Margin="2,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>
                </c:Aspect>

                <Grid Margin="1,8,1,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Game}" FontSize="12.5" FontWeight="Medium" TextTrimming="CharacterEllipsis"
                               MaxWidth="150"/>
                    <!-- «новый» у только что сохранённых: рядом с названием, а не поверх кадра -->
                    <Border Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,0,0,0"
                            Background="{DynamicResource AccentSoftBrush}" CornerRadius="4" Padding="5,0,5,1"
                            Visibility="{Binding NewVisibility}">
                        <TextBlock Text="новый" FontSize="10.5" FontWeight="SemiBold" Foreground="{DynamicResource AccentTxBrush}"/>
                    </Border>
                    <TextBlock Grid.Column="2" Text="{Binding CardTime}" FontSize="12" Margin="8,0,0,0"
                               Foreground="{DynamicResource Tx3Brush}" FontFamily="{DynamicResource DispFont}"/>
                </Grid>
                <TextBlock Text="{Binding CardDetail}" FontSize="12" Margin="1,2,1,0"
                           Foreground="{DynamicResource Tx3Brush}" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Button>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter TargetName="SelectionRing" Property="Opacity" Value="1"/>
                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
            </DataTrigger>
            <Trigger SourceName="Shot" Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ShotShift" Storyboard.TargetProperty="Y"
                                             To="-2" Duration="0:0:0.28" EasingFunction="{StaticResource Spring}"/>
                            <DoubleAnimation Storyboard.TargetName="ShotScale" Storyboard.TargetProperty="ScaleX"
                                             To="1.02" Duration="0:0:0.28" EasingFunction="{StaticResource Spring}"/>
                            <DoubleAnimation Storyboard.TargetName="ShotScale" Storyboard.TargetProperty="ScaleY"
                                             To="1.02" Duration="0:0:0.28" EasingFunction="{StaticResource Spring}"/>
                            <DoubleAnimation Storyboard.TargetName="Play" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetName="PlayScale" Storyboard.TargetProperty="ScaleX"
                                             To="1" Duration="0:0:0.3" EasingFunction="{StaticResource Spring}"/>
                            <DoubleAnimation Storyboard.TargetName="PlayScale" Storyboard.TargetProperty="ScaleY"
                                             To="1" Duration="0:0:0.3" EasingFunction="{StaticResource Spring}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ShotShift" Storyboard.TargetProperty="Y"
                                             To="0" Duration="0:0:0.28"/>
                            <DoubleAnimation Storyboard.TargetName="ShotScale" Storyboard.TargetProperty="ScaleX"
                                             To="1" Duration="0:0:0.28"/>
                            <DoubleAnimation Storyboard.TargetName="ShotScale" Storyboard.TargetProperty="ScaleY"
                                             To="1" Duration="0:0:0.28"/>
                            <DoubleAnimation Storyboard.TargetName="Play" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.18"/>
                            <DoubleAnimation Storyboard.TargetName="PlayScale" Storyboard.TargetProperty="ScaleX"
                                             To="0.82" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetName="PlayScale" Storyboard.TargetProperty="ScaleY"
                                             To="0.82" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!-- Сетка карточек с переносом -->
    <ItemsPanelTemplate x:Key="ClipGridPanel">
        <WrapPanel Orientation="Horizontal" ItemWidth="222"/>
    </ItemsPanelTemplate>

</ResourceDictionary>
